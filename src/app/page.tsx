// This is the starting point where we shows the landing page
// Landing page include the primary sections like Hero, Features, Product, Pricing, Faq, and Footer components.

import LandingPage from "@/components/Landing-page-10";
export default async function Home() {
  return (
    <>
      <LandingPage />
    </>
  );
}
